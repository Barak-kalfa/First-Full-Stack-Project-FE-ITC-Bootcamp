import Button from "react-bootstrap/Button";

function WishListButton() {
     return (
          <div>
               <Button variant="info">Save For Later</Button>
          </div>
     );
}

export default WishListButton;
